# scraperecon

A Python CLI tool that runs a sequential reconnaissance pipeline against a target URL before a developer writes a scraper. It tells you what bot protection a site has, whether plain HTTP or TLS impersonation is sufficient, and how aggressively it rate limits.

## Installation

```bash
pip install scraperecon
```

## Usage

```bash
scraperecon https://target.com
```
